import * as path from "path";

export const API_URL = "";
export const suggestionFolderPath = path.join(
  __dirname,
  "../public/suggestions"
);

export const MAX_TG_MESSAGE_LENGTH = 900;

export const postsInfoPath = path.join(__dirname, "../public/posts");
export const ENCODING_FORMAT = "utf-8";
